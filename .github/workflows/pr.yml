name: PR Build

on:
    pull_request:
        branches:
            - master
            - dev

jobs:
    build:
        uses: LSS-Manager/LSSM-V.4/.github/workflows/build.yml@pull/1771
        secrets: inherit
