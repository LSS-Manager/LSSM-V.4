name: PR Build

on:
    pull_request:
        branches:
            - master
            - dev

jobs:
    - name: Build
      uses: LSS-Manager/LSSM-V.4/.github/workflows/reusable-workflows/build.yml@master
      secrets: inherit
